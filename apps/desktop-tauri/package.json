{
  "name": "dsh-desktop",
  "version": "0.1.6-alpha.2",
  "private": true,
  "type": "module",
  "scripts": {
    "prep": "bash scripts/sync-version.sh && bash scripts/prepare-runtime.sh",
    "build": "bash scripts/sync-version.sh && npx -y @tauri-apps/cli@2 build --bundles dmg"
  }
}
